import 'package:flutter/material.dart';

class CustomerPostJobsView extends StatelessWidget {
  const CustomerPostJobsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Customer Post Jobs")));
  }
}
